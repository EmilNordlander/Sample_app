<li>
<%= gravatar_for user, size: 50 %> 
<%= link_to user.name, user %>

<% if current_user.admin?  %>
<%= button_to "Ta bort", user, :method => :delete, data: {confirm: "Är du säker?"} %>
</li>
<% end %>